name: dotnet-iis_module-UriCacheModule
filetype: config$
target: line
type: contains
advice: Implements a generic cache for URL-specific server state; such as configuration.
  With this module; the server only reads configuration for the first request for
  a particular URL. And reuse it on subsequent requests until it changes.
effort: 1000
readiness: 1000
category: Unsupported IIS modules
patterns:
- value: UriCacheModule
